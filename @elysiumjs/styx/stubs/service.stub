import { LoggerService, Service, ServiceScope } from '@elysiumjs/core';

@Service.register({ name: '{alias}', scope: ServiceScope.{scope} })
export class {name} {
    private static readonly logger = LoggerService.make({name}.name);

    public constructor() {}
}
