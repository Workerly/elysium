import { Repository, Service, ServiceScope } from '@elysiumjs/core';

import { {model} } from '#{module}/models/{model_name}.model';

@Service.register({ name: '{alias}', scope: ServiceScope.{scope} })
export class {name} extends Repository({model}) {}
